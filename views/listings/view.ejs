<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Listing Details</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 40px auto;
      background-color: #fff;
      padding: 30px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.1);
      border-radius: 10px;
    }

    .listing-image {
      display: block;
      margin: 20px auto;
      max-width: 100%;
      height: auto;
      border-radius: 10px;
      object-fit: cover;
    }

    h2 {
      margin: 10px 0;
      color: #333;
    }

    .details {
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-top: 20px;
    }

    .details span {
      font-weight: bold;
      color: #555;
    }

    .buttons {
      margin-top: 30px;
      display: flex;
      gap: 15px;
    }

    .buttons button {
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
      transition: all 0.3s ease;
    }

    .buttons button:hover {
      opacity: 0.9;
    }

    .edit-btn {
      background-color: #4CAF50;
      color: white;
    }

    .delete-btn {
      background-color: #f44336;
      color: white;
    }

    @media (max-width: 600px) {
      .container {
        margin: 20px;
        padding: 20px;
      }
      .buttons {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>

  <div class="container">
    <h1><%= listed.title %></h1>
    <img class="listing-image" src="<%= listed.image.url %>" alt="Image not loaded">

    <div class="details">
      <h2>Price: <span>$<%= listed.price %></span></h2>
      <h2>Location: <span><%= listed.location %></span></h2>
      <h2>Country: <span><%= listed.country %></span></h2>
    </div>

    <div class="buttons">
      <form method="get" action="/listings/<%= listed._id %>/edit">
        <button type="submit" class="edit-btn">Edit</button>
      </form>

      <form method="post" action="/listings/<%= listed._id %>?_method=DELETE">
        <button type="submit" class="delete-btn">Delete</button>
      </form>
    </div>
  </div>

</body>
</html>
